<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <title>List</title>
</head>
<body style="margin: auto; padding:10%; background-color: beige;" >
    <h2>{{title}}</h2>
    <table class="table  table-striped table-hover table-responsive">
        <tr>
            <th>Song Id</th>
            <th>Artist Name</th>
            <th>Price</th>
            <th>Song</th>
            <th>Action</th>
        {{#each song}}
            <tr>
                <td>{{this.id}}</td>
                <td>{{this.name}}</td>
                <td>{{this.price}}</td>
                <td>{{this.song}}</td>
                <td>
                    <a href="/auth/updatesong/{{this.id}}">Edit</a> 
                    <a href="/auth/delete_song/{{this.id}}" onclick="return confirm('Are you sure you want to delete this record?')">Delete</a>
                </td>
                
            </tr>
        {{/each}}
    </table>
    {{#if message}}
    <h4>{{message}}</h4>
    {{/if}}
    <a href="/">BACK HOME</a>
</body>
</html>